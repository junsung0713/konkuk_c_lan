#include <stdio.h>
#include <math.h>
int main(void){
    double x = 3.24;
    printf("%15.2f",x);
    // double y,x ,a,b;
    // y = (sqrt((pow(a,2) - pow(b,2) )) )/((3*x) +1);
    // char a;
    // printf("Enter a single character: ");
    // scanf("%c", &a);
    // if( a >= 'A' & a <='Z' ){
    //     printf("%c -> Capital Letter: %c \n", a, (a + 'a' - 'A'));
    // } else if ( a >= 'A' & a <= 'Z'){
    //     printf("%c -> Ascii Code: %d \n", a, a);
    // } else {
    //     printf("%c -> 알파벳이 아닙니다.", a);
    // }
    // return 0;
}